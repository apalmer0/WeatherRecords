import { StyleSheet } from 'react-native'

const TEXT_COLOR = '#EBECEE'

export default StyleSheet.create({
  tempsToday: {
    color: TEXT_COLOR,
    textAlign: 'right',
  },
  tempsTodayHeader: {
    color: TEXT_COLOR,
  },
})
