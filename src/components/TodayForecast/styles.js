import { StyleSheet } from 'react-native'

const TEXT_COLOR = '#EBECEE'

export default StyleSheet.create({
  colStyle: {
    height: 30,
  },
  gridStyle: {
    flex: 0,
  },
  tempsToday: {
    color: TEXT_COLOR,
    textAlign: 'center',
  },
  tempsTodayHeader: {
    color: TEXT_COLOR,
    textAlign: 'center',
  },
})
